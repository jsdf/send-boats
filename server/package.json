{
  "name": "send-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "./scripts/deploy-with-version.sh",
    "dev": "wrangler dev --ip 0.0.0.0",
    "start": "npm run dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.4",
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.5.2",
    "vitest": "2.1.8",
    "wrangler": "^4.14.1"
  }
}
